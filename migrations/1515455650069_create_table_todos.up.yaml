- args:
    sql: CREATE TABLE "todos"("id" serial NOT NULL, "text" text NOT NULL, "completed"
      boolean NOT NULL, "user_id" integer NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: todos
  type: add_existing_table_or_view
